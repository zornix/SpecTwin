# Optional: point the web app at the FastAPI matching service.
# When unset, the app uses its built-in TypeScript matcher (no Python needed).
# Copy to .env.local and adjust as needed.
MATCHING_SERVICE_URL=http://localhost:8000
